# [문서4] 네트워크 확인 질문 — 핵심 점검용
> 문서1 각 개념을 듣고 나올 후속 질문. 깊이보다 "핵심을 제대로 짚었나" 재확인용.
> 이해했다면 1~2문장으로 바로 답할 수 있어야 함. (답은 문서1·2에)

---

## 1. 계층 모델
- L2와 L3의 차이를 한 문장으로? (누구까지 전달하나)
- 통신 중 IP와 MAC 중 홉마다 바뀌는 건? 고정인 건?
- "ping은 되는데 접속이 안 된다" → 어느 계층은 살아있고 어느 계층을 의심?
- 캡슐화에서 TCP 세그먼트에 IP 헤더가 붙으면 뭐라고 부르나?

## 2. TCP 핸드셰이크 & 상태
- 왜 2-way가 아니라 3-way인가?
- 4-way는 왜 4번인가? (3-way처럼 못 합치는 이유)
- TIME_WAIT은 연결을 먼저 끊은 쪽? 나중에? 왜 대기하나?
- CLOSE_WAIT이 계속 쌓인다 → 무엇을 의심? (커널? 앱?)
- TIME_WAIT과 CLOSE_WAIT 중 앱이 안 고치면 영구히 남는 건?
- ISN을 랜덤으로 두는 이유 하나?

## 3. 흐름제어 vs 혼잡제어
- 흐름제어와 혼잡제어는 각각 "누구"를 배려하나?
- 실제 송신량은 rwnd와 cwnd 중 무엇으로 정해지나?
- Zero Window가 뜨는 상황은? 이게 무슨 개념의 구현인가? (backpressure)
- 대역폭은 남는데 처리량이 안 나온다 → 무슨 공식을 의심? (BDP)
- BDP를 말로 풀면? (대역폭 × RTT = ?)
- CUBIC과 BBR의 근본 차이? (손실 기반 vs 대역폭 기반)
- HOL Blocking을 해결한 프로토콜과 그 방법?

## 4. MTU/MSS · 지연 범인
- MTU와 MSS의 차이? MSS는 어떻게 계산? (1500−40=?)
- MSS는 언제 서로 합의하나? (핸드셰이크 어느 단계)
- VPN 켜면 특정 사이트만 안 열림 → 원인과 해법?
- "작은 요청은 되는데 큰 응답만 멈춤" → 무슨 문제? (PMTUD 블랙홀)
- 요청 응답이 이상하게 40ms씩 밀림 → 무슨 조합? 해법은?
- 대역폭은 나오는데 지연이 널뜀 → 무엇? (Bufferbloat)

## 5. TLS · HTTPS
- TLS가 보장하는 3가지?
- 비대칭키와 대칭키의 역할 분담을 한 문장으로?
- RSA 키교환과 ECDHE의 차이? 왜 ECDHE가 과거 트래픽에 안전한가?
- Forward Secrecy를 한 문장으로?
- ECDHE로 키를 만드는데 인증서(개인키)는 대체 뭐에 쓰나?
- MITM은 왜 못 뚫나? (무엇이 막나)
- SNI는 왜 필요한가? (언제 문제가 되나)
- TLS 1.2와 1.3의 RTT 차이? 1.3이 제거한 것?
- 사내 프록시가 HTTPS를 까볼 수 있는 이유?
- TLS 핸드셰이크에서 서명 검증이 두 번 나온다 — 각각 무슨 키로, 무엇을 검증하나? (①CA 공개키로 인증서 진위 ②서버 공개키로 인증서 주인+DH값)
- 데이터 무결성 검증에 공개키가 쓰이나? (아니다 — 대칭키로 HMAC/AES-GCM 태그)
- 비대칭키와 대칭키는 핸드셰이크의 어느 구간에서 각각 쓰이나? (비대칭=앞 인증, 대칭=핸드셰이크 후 데이터)
- DH만으로는 왜 중간자를 못 막나? 무엇이 그걸 보완하나? (CertificateVerify가 DH값까지 서명)

## 6. URL 입력 후 벌어지는 일
- DNS 조회 순서를 큰 틀로? (캐시 → ... → 권한 NS)
- ARP는 무엇을 알아내나? (무엇→무엇 변환)
- 전체 흐름을 계층으로 매핑하면? (DNS→ARP→TCP→TLS→HTTP)
- 이 흐름에서 지연이 생길 수 있는 지점 3개?

## 7. VPN
- VPN의 핵심 동작을 한 문장으로? (캡슐화 + 암호화)
- VPN 켜면 왜 MTU가 줄어드나? 그 부작용은?
- VPN이 주는 효과 3가지?

## 8. 프록시 vs 리버스 프록시
- 포워드와 리버스의 차이를 한 문장으로? (누구를 숨기나)
- 리버스 프록시의 대표 역할 3가지?
- TLS 종료(termination)가 무슨 뜻? 뒤 실서버는 어떤 상태로 통신?
- 클라이언트는 리버스 프록시를 무엇으로 인식하나?

---

## 9. 스위치·라우터·MAC·ARP·DNS
- 스위치와 라우터의 차이를 한 문장으로? (L2 vs L3)
- 같은 네트워크인지 아닌지는 무엇으로 판단하나? (서브넷 마스크)
- ARP는 무엇을 무엇으로 변환하나?
- 밖으로 나갈 때 ARP로 찾는 MAC은 목적지 서버 것? 게이트웨이 것?
- 통신 중 IP와 MAC 중 홉마다 바뀌는 건?
- DNS의 재귀와 반복 질의 차이?
- DNS 조회 캐시 계층 순서를 큰 틀로?
- TTL을 낮추면 뭐가 좋고 뭐가 나쁜가?
- traceroute는 무엇을 이용해 경로를 알아내나? (TTL + ICMP)
- "ping이 실패했다" → 서비스가 죽은 거라고 단정할 수 있나?

## 10. 실전편 (경험 기반)
- 401과 403의 차이? (인증 vs 인가)
- 502와 504의 차이? 트러블슈팅 방향이 어떻게 다른가?
- 429는 무슨 상황?
- 네트워크 장애 시 "IP는 되는데 도메인만 안 된다" → 무슨 문제로 좁히나?
- ping은 되는데 서비스 접속이 안 되면 다음 확인은? (포트 → telnet/nc)
- ping -M do -s 로 뭘 테스트하나? (MTU)
- 리버스 프록시(Nginx)의 역할 3가지? TLS 종료란?
- SSH 공개키 인증 원리를 한 문장으로? 포트 포워딩은 언제 쓰나?
- TCP는 스트림이라 생기는 문제 하나? (메시지 경계 없음 → 프레이밍)
- 프레이밍 방법 2가지? (길이 프리픽스 / 구분자)
- recv(1024)가 항상 1024를 주나? 아니면 어떻게 대응?
- C10K 문제와 해법? (스레드per연결 불가 → epoll 이벤트 기반)
- WebSocket이 HTTP와 다른 결정적 차이? (서버가 먼저 push)
- 왜 채팅에 폴링 대신 WebSocket인가?
- 채팅 서버가 여러 대일 때 인스턴스 간 메시지 전파는? (Redis Pub/Sub)
- 끊긴 사이 온 메시지를 어떻게 복구? (DB 저장+커서)
- gRPC 스트리밍 4종 중 실시간 채팅에 맞는 것? (양방향)
- REST/gRPC/GraphQL을 각각 언제 쓰나?
- gRPC가 빠른 이유 2가지? (HTTP/2 + Protobuf 바이너리)
- GraphQL의 대표 함정 2가지? (N+1, 쿼리 복잡도)
- RPC를 한 문장으로? (원격 함수를 로컬처럼 호출)

## 11. 실전 사례 추가분
- 클라우드 서버는 왜 비번 대신 pem 키로 접속하나?
- PuTTY에서 pem을 바로 못 쓰는 이유? (ppk 변환 필요)
- "키 맞는데 접속 거부" → 무엇을 의심? (권한 chmod 400)
- WAS와 DB를 왜 분리하나? 3가지?
- 3-tier에서 DB 서버 방화벽은 어떻게 여나?
- 인바운드와 아웃바운드 차이? 방화벽 기본 정책은 보통?
- UFW enable 전에 반드시 열어야 하는 포트? 왜?
- "UFW는 열었는데 접속 안 됨" 클라우드에서 → 무엇 확인? (보안그룹)
- 정적 페이지와 동적(SPA) 스크래핑의 차이와 도구?
- "브라우저엔 보이는데 requests엔 안 잡힘" → 원인과 고급 해법?
- CORS 에러는 서버가 막나 브라우저가 막나?
- curl은 되는데 브라우저만 실패 → 무슨 문제?
- application/json 요청이 preflight를 유발하는 이유?
- 단순 요청(preflight 없는) 조건 3가지?
- credentials 쓸 때 Allow-Origin에 * 못 쓰는 이유?
- 트래픽 급증 대응을 계층별로 나열하면?
- Circuit Breaker는 무엇을 막나? 큐잉은 무슨 개념의 구현? (backpressure)
- 캐싱 세 계층(CDN/HTTP헤더/Redis)의 역할 분담?
- Cache-Control의 public과 private 차이? no-cache와 no-store 차이?
- cache-aside 패턴을 한 문장으로? 데이터 수정 시 반드시 해야 할 것? (무효화)
- 서버 여러 대일 때 세션을 왜 Redis에 두나?
- 사용자별 데이터를 public 캐싱하면 무슨 사고? Cache Stampede란?

## 12. AWS
- IAM의 4요소는? (User/Group/Role/Policy)
- 왜 액세스 키(User)보다 Role을 쓰나?
- EC2에서 S3 접근 시 자격증명을 어떻게 주나? (Role/Instance Profile)
- IAM 권한 평가 규칙? (명시적 Deny > Allow, Allow 없으면 암묵적 Deny)
- SG와 NACL의 차이 3가지? (레벨/Allow만vsDeny/stateful vs stateless)
- SG가 stateful이라는 게 무슨 뜻? NACL은 왜 양방향을 다 열어야 하나?
- 퍼블릭 서브넷과 프라이빗 서브넷의 차이? DB는 어디 두나?
- 프라이빗 서브넷이 밖으로 나갈 때 뭘 거치나? (NAT 게이트웨이)
- IP 하드코딩 대신 화이트리스트를 어떻게 짜나? (SG가 SG 참조)
- Site-to-Site VPN과 Client VPN 차이?
- SSM Session Manager가 베스천보다 나은 점? (22 포트 안 열어도 됨)
- Presigned URL이 뭐고 언제 쓰나?
- S3 접근은 어느 두 정책에서 통제되나? (IAM + 버킷 정책)

## 11-B. API 통신 디버깅 (개발자 관점)
- API 응답이 이상할 때 제일 먼저 보는 것? (상태 코드)
- 4xx와 5xx 중 내 코드를 봐야 하는 건? 서버 로그를 봐야 하는 건?
- "curl은 되는데 내 코드는 안 됨" → 무슨 문제?
- 개발자도구에서 요청을 코드 밖으로 그대로 복제하는 법? (Copy as cURL)
- 내 서버가 응답을 못 줄 때, 요청이 도달했는지 어떻게 확인? (액세스 로그)
- 500이 떴다 — 원인을 어디서 찾나? (에러 로그 스택 트레이스)
- try-catch로 예외를 삼키면 왜 최악인가?
- 타임아웃 체인이 뭔가? (클라 타임아웃 < 서버가 DB/외부 기다리는 시간)
- 분산 환경에서 한 요청을 관통 추적하는 법? (correlation ID)

## 종합 (한 흐름으로 답하기)
- "TCP 통신의 전체 생애주기를 연결→전송→종료로 설명해보라."
- "HTTPS 접속을 DNS부터 데이터 수신까지 한 흐름으로 설명해보라."
- "느린 네트워크 트러블슈팅, 어느 계층부터 어떻게 좁혀가나?"
- "패킷 하나가 브라우저에서 원격 서버까지 가는 여정을 IP/MAC 관점으로 설명해보라."
- "네트워크 장애 신고를 받았다. 어떤 순서로 원인을 좁혀가나?"
- "클라우드에 웹/WAS/DB를 3-tier로 올리고 보안까지 잡는 과정을 설명해보라."
- "트래픽이 평소의 100배로 급증했다. 어떤 순서로 대응하나?"
- "AWS에 3-tier 웹서비스를 올리고 보안(네트워크+권한)까지 설계해보라."
- "API 응답이 안 온다. 클라이언트 개발자로서 어떤 순서로 원인을 좁히나?"
- "내 서버가 특정 요청에 500을 뱉는다. 어떻게 추적하나?"
