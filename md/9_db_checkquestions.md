# [문서9] DB 확인 질문 — 핵심 점검용
> 문서6~8 각 개념을 듣고 나올 후속 질문. "핵심을 제대로 짚었나" 재확인용.
> 이해했다면 1~2문장으로 바로 답할 수 있어야 함.

---

## 1. 인덱스
- 인덱스가 없으면 어떻게 검색하나? 있으면 복잡도는?
- 왜 이진트리가 아니라 B+Tree를 쓰나? (2가지)
- B+Tree가 범위 검색에 강한 이유?
- 해시 인덱스가 기본이 아닌 이유?
- 클러스터형과 논클러스터형 차이? 테이블당 클러스터는 몇 개?
- 세컨더리 인덱스의 2단계 조회란? 이게 왜 커버링 인덱스로 이어지나?
- 인덱스의 단점 2가지?
- 인덱스가 안 타는 경우 3개? 공통 원칙은?
- 풀스캔이 인덱스보다 나은 경우? 대략 몇 % 기준?

## 2. 쿼리 최적화
- EXPLAIN에서 type=ALL이 뜻하는 것?
- 커버링 인덱스를 한 문장으로?
- 복합 인덱스 (a,b,c)에서 WHERE b=2는 타나? 왜?
- 등호와 범위 조건, 복합 인덱스에서 어느 걸 앞에 두나?
- "어제까진 빨랐는데 오늘 느림" — 무엇을 의심? (통계)
- 통계 갱신이 위험할 수 있는 이유 2가지?
- 조인 알고리즘 3가지와 각각 언제?
- 조인 키에 인덱스가 없으면?

## 3. 트랜잭션/동시성
- ACID 각각 한 단어로?
- 원자성·지속성은 무엇으로 구현? (WAL)
- 격리 수준 4단계와 각각 막는 이상 현상?
- MySQL과 PostgreSQL 기본 격리 수준?
- MVCC를 한 문장으로? "읽기는 ___을 안 건다"?
- 데드락 예방법?
- 비관적 락과 낙관적 락 차이? 각각 언제?

## 4. 아키텍처
- Database와 Instance 차이?
- SGA와 PGA 차이? 정렬/해시는 어디서?
- 백그라운드 프로세스 5개와 역할? (DBWR/LGWR/CKPT/SMON/PMON)
- commit 순간 가장 중요한 프로세스는? (DBWR? LGWR?)
- WAL에서 로그와 데이터 중 뭐가 먼저?
- Fast Commit의 의미?
- 버퍼 상태 3종? Dirty가 많으면 뭘 의심?
- 커넥션 풀이 왜 필요? 크기는 크면 좋은가?
- DB 기동 3단계와 각 단계 역할? 실패 단계로 뭘 진단?

## 5. 데이터 모델링
- 정규화가 막는 이상 현상 3가지?
- 3NF를 한 문장으로?
- 반정규화는 무엇을 얻고 무엇을 잃나?
- N:N을 어떻게 표현? 중간 테이블의 진짜 이점?
- 자연 키와 대리 키 차이? 실무 기본은?
- auto_increment와 UUID의 트레이드오프?
- 금액에 FLOAT를 쓰면 안 되는 이유?
- TIMESTAMP와 DATETIME 차이?
- 타입을 작게 쓰면 왜 성능에 유리?

## 6. 실전
- N+1 문제란? 어떻게 해결?
- N+1이 로컬에서 안 보이는 이유?
- 슬로우 쿼리 튜닝 순서?
- 복제에서 읽기/쓰기 분리 원리? 복제 지연 문제는?
- 샤딩과 파티셔닝 차이? 샤드 키가 왜 중요?
- ORM 함정 3가지?
- OFFSET 페이지네이션이 느린 이유? 대안은?

## 6-B. 기초
- DDL/DML/DCL/TCL 구분?
- TRUNCATE와 DELETE 차이?
- SELECT 논리 실행 순서? WHERE와 HAVING 차이?
- NULL 비교는 어떻게? COUNT(*)와 COUNT(col) 차이?
- INNER/LEFT/FULL JOIN 차이?
- RDB와 NoSQL 선택 기준?

## 종합
- "인덱스를 설계하고 쿼리를 튜닝하는 전체 과정을 설명해보라."
- "트랜잭션이 커밋되고 장애가 나도 데이터가 살아남는 원리를 설명해보라."
- "목록 API가 느리다. 어떤 순서로 원인을 찾고 개선하나?"
